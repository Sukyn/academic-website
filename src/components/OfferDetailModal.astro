---
type Props = {
  id: string;
  labelledBy: string;
};

const { id, labelledBy } = Astro.props as Props;
---

<dialog
  id={`${id}-dialog`}
  class="offer-modal"
  data-offer-modal={id}
  aria-labelledby={labelledBy}
>
  <div class="offer-modal__surface">
    <div class="offer-modal__topbar">
      <button
        type="button"
        class="offer-modal__close"
        data-offer-modal-close
        aria-label="Close details"
      >
        Close
      </button>
    </div>

    <div class="offer-modal__content">
      <slot />
    </div>
  </div>
</dialog>

<style>
  .offer-modal {
    width: min(52rem, calc(100% - 2rem));
    max-width: 52rem;
    padding: 0;
    border: none;
    background: transparent;
    color: inherit;
  }

  .offer-modal::backdrop {
    background: rgba(0, 0, 0, 0.35);
  }

  .offer-modal__surface {
    max-height: 85vh;
    overflow: auto;
    padding: 1rem 1.25rem 1.25rem;
    border: 1px solid var(--color-border-strong);
    background: var(--color-bg);
  }

  .offer-modal__topbar {
    display: flex;
    justify-content: flex-end;
    margin-bottom: 1rem;
  }

  .offer-modal__close {
    appearance: none;
    padding: 0.3rem 0.65rem;
    border: 1px solid var(--color-border-strong);
    background: var(--color-surface-2);
    color: var(--color-text);
    cursor: pointer;
  }

  .offer-modal__close:hover {
    background: var(--color-surface);
  }

  .offer-modal__close:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
  }

  .offer-modal__content {
    position: relative;
  }
</style>
